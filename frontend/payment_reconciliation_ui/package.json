{
  "name": "payment_reconciliation_ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Settlement Reconciliation Console — React frontend over the payment reconciliation API",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^2.1.9",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
